#include <bits/stdc++.h>

using namespace std;
#define ii pair<int, int>

int main() {
	int t; cin >> t;
	while (t--) {
		int N, M, L; cin >> N >> M >> L;
		vector<ii> prices(L);
		vector<string> names(L);

	}
}
