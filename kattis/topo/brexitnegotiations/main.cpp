#include <bits/stdc++.h>

using namespace std;

int n;
vector<int> M;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cin >> n;
	M.assign(n, 0);
	for (int i = 0; i < n; i++) {
		int d; cin >> M[i] >> d;
		while (d--)

	}
}
