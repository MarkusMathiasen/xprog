#include <bits/stdc++.h>
using namespace std;

int main() {
	int T; scanf("%d", &T);
	for (int t = 1; t <= T; t++) {
		int n; scanf("%d", &n);
		unordered_map<int, int> written;
		graph.
		for (int j = 0; j < n; j++) {
			int m; scanf("%d", &m);
			for (int i = 0; i < m-1; i++) {
				int x; scanf("%d", &x);
				read[x].insert(j);
			}
			int x; scanf("%d", &x);
			written[x].insert(j);
		}
		for (auto& [k,v] : 
	}
}
